package com.metalac.scanner.app.view.inventorylist.interfaces;

import com.metalac.scanner.app.models.InventoryList;

public interface IOnInventoryListClickCallback {
    void onClick(InventoryList inventoryList);
}
